# Railway configuration file - Build and run proxy server
[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r requirements.txt && cd .. && npm install && npm run build"
startCommand = "bash start.sh"

[variables]
NODE_ENV = "production"