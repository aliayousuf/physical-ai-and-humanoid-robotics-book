# Railway configuration file for Docusaurus Frontend
[build]
builder = "nixpacks"
buildCommand = "npm run build"
startCommand = "serve -s build --listen ${PORT:-3000}"

[variables]
NODE_ENV = "production"