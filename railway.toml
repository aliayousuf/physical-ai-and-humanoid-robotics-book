# Railway configuration file for Backend API
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'python -m uvicorn backend.src.api.main:app --host 0.0.0.0 --port ${PORT:-8000}'"

[variables]
NODE_ENV = "production"